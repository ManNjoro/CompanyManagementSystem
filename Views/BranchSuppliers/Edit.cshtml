@model BranchSupplier

@section title {
    @if (Model != null)
    {
        <h3>Supplier: @Model.SupplierName</h3>
    }
}

@if (Model != null)
{
    <partial name="_BranchSuppliers" model="@Model" />
}