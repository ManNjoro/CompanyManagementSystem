@model WorksWith

@section title {
    @if (Model != null)
    {
        <h3>Record: @Model.EmpId - @Model.ClientId</h3>
    }
}

@if (Model != null)
{
    <partial name="_WorksWiths" model="@Model" />
}